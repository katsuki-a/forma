# forma — 作業規約

日本語で応答・記述する。コードの識別子とコミットメッセージは英語を基本とする。

## 役割と判断

- PdMが利用者の課題、プロダクトの範囲、機能の採否、優先順位を決める。
- AIは意図の整理、調査、選択肢の提示、仕様への変換、実装、検証を担う。AI自身の提案をPdMの決定として扱わない。
- 既に承認された範囲の実装手順や可逆な保守作業は進める。新しい機能・制約・優先順位を導入する場合は提案として残し、PdMの採択前に現行仕様や実装へ取り込まない。

## 情報の所在

| 場所 | 正とする情報 | 入れないもの |
| --- | --- | --- |
| `docs/` | PdMの意図、背景、制約、未決の問い、機能の採否 | AIによる根拠のない決定の代筆 |
| `spec/` | 承認された意図から導く現行の振る舞い・不変条件・確認条件 | 調査、経緯、候補、将来の機能一覧、特定ライブラリの詳細 |
| `adr/` | 日付付きの調査、比較、選択理由、結果、過去の判断 | 現行仕様の複製 |
| `README.md` | 製品の目的、実装状況、実行できる手順、ドキュメントへの入口 | 開発者個人の動機、未実装機能を利用可能とする説明 |

開発者個人の動機は `docs/` にだけ置く。他の場所には必要な技術制約や構成だけを書く。`docs/` は `.gitignore` で除外するローカル専用資料で、コミット・pushしない。`git add -f` で追加したり、内容を公開先へコピーしたりしない。認証情報はこの資料にも保存しない。

## 更新の手順

1. 関連するintentと現行仕様を読み、既存の決定・作業範囲を確認する。
2. 新しいPdMの発言は `docs/` に出典日とともに記録する。確定事項と候補を分け、要約に推測を混ぜない。新規提案は `proposed` とする。
3. 仕様を変える根拠が承認済みなら、最小の差分で `spec/` を更新する。各仕様項目に一意なID、承認済みintentのID、観測可能な確認条件を付ける。根拠から導けない点は `docs/` の問いに戻す。
4. 技術選択や比較に記録する価値があるときだけADRを追加する。細かな編集ごとにADRを増やさない。
5. 対応する仕様IDを確認して実装・検証する。振る舞いを変える実装では、対応する確認条件に基づくテストを用意する。
6. `npm run check` と `git diff --check` を実行する。管理者のローカルでは `npm run check:local` で非公開intentも照合する。差分を見て矛盾・重複・未実装の誇張がないか確認する。

過去の記録と現行仕様が食い違うときは、まず状態と承認根拠を確認する。承認済みの新しいPdMの指示は対応するintentとspecに反映する。古いADRや単に新しい日付のAI文書で現行仕様を上書きしない。

## 文書を増やしすぎない

- 一つのルールの定義元は一か所。別の文書ではリンクする。
- `spec/` は常に現行版だけを保つ。変更前の全文はGit履歴で追跡する。変更・廃止した仕様IDを別の意味で再利用しない。
- intentの意味を変える決定は新しい記録にし、旧intentを `superseded` にして後継を示す。誤字の修正はその場でよい。
- ADRは `proposed` / `accepted` / `rejected` / `superseded` の状態を持つ。旧判断は削除せず、後継へのリンクを足す。調査は陳腐化してよく、再利用時に一次情報を再確認する。
- 比較対象の製品名や模倣を連想させる位置づけを、README、リポジトリ説明、UI、現行仕様などの対外的な製品説明に出さない。製品の目的はforma自身の言葉で説明する。
- 比較対象を明示する機能選定資料は `docs/` に置く。その内容は候補材料であり、formaの要件ではない。採用する場合も、固有の製品名を使わず利用者に必要な振る舞いとしてspecに定義する。
- メタデータの書式は各ディレクトリのREADMEに従う。テンプレートや管理台帳を目的なく増やさない。

## 検査と人による確認の境界

公開CIは、文書の構造、IDの一意性、公開リンク先ファイル、specの根拠IDの書式、docsがGit管理されていないことを確認する。非公開intentの承認状態はローカル検査だけで照合する。承認の真正性、意味の整合性、仕様の十分性・非冗長性は自動検査では保証しない。

公開文書は非公開資料へのファイルリンクを持たず、必要な場合はintentのIDだけを記す。ローカルintentを読めない環境では承認済みと推測せず、仕様変更の根拠は管理者が確認する。既存の公開仕様で完結する作業は進められる。

specを変更する差分では、次を人が確認できるようにする。

- どのPdMの意図から、どの仕様を追加・変更・削除したか。
- 同じ意味を別の場所に定義していないか。
- 未決事項やAIの推測を事実として持ち込んでいないか。
- 何を検証し、何が未検証か。

検査に通すために仕様・承認記録・テストの期待値を都合よく変更しない。規約や検査の緩和が必要な場合は理由と影響を示す。

## 公開品質とGit

- 秘密鍵、トークン、実在の業務データ、個人のローカルパス、未公開情報をコミットしない。サンプルは架空の値を使う。
- 未実装の機能や検証していない手順を利用可能と記述しない。構成が変わったらREADMEも更新する。
- 依存関係は必要な時点で導入し、lockfileをコミットする。Workersで動かすコードにNode.js固有機能を持ち込む場合は互換性を確認する。
- 関係のない変更に触れない。通常のブランチ名は `codex/` を接頭辞にする。
- コミット・push・公開・デプロイは依頼された範囲で行う。GitHubへのpushとCloudflareへのデプロイは別の操作として扱う。
- 完了時は変更内容、検証結果、残っている未決事項を簡潔に報告する。
